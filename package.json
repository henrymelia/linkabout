{
  "name": "linkabout",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "start": "node apps/api/dist/main"
  },
  "devDependencies": {
    "turbo": "^1.10.12"
  }
}
